CREATE TABLE car_repair
(
    id                 INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    status             VARCHAR(255),
    car                INTEGER,
    repair_start_time  TIMESTAMP WITHOUT TIME ZONE,
    repair_finish_time TIMESTAMP WITHOUT TIME ZONE,
    CONSTRAINT pk_car_repair PRIMARY KEY (id)
);

ALTER TABLE car_repair
    ADD CONSTRAINT FK_CAR_REPAIR_ON_CAR FOREIGN KEY (car) REFERENCES car (id);